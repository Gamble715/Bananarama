# Public name of this content type
name: Recipes

# TODO: explain
slug: recipes

# Explanation for the backoffice
description: A description of the content type for the editors

# Default field (e.g title)
label_field_name:  title

# Order of entries
order_by: manually # default: manually, also available: created_at or any field slug

# Order direction
# order_direction: asc # default: asc, also available: desc

# Display entries grouped by <field> in the backoffice
group_by: category

# Activate public 'create' API (e.g for a contact form)
# public_submission_enabled: false

# Emails to be notified on new entries using the public API
# public_submission_accounts: ['john@acme.net']

# Describe each field. The name shoud always be an underscored string
fields:
-  image: # Name of the field
    label:  image
    type: file
    required: true
    hint: A description of the field for the editors
    localized: false

- title: # Name of the field
    label: Title
    type: string
    required: false
    hint: A description of the field for the editors
    localized: false

- minutes: # Name of the field
    label: Minutes
    type: integer
    required: false
    hint: A description of the field for the editors
    localized: false

- ingredients: # Name of the field
    label: Ingredients
    type: integer
    required: false
    hint: A description of the field for the editors
    localized: false

- description: # Name of the field
    label: Description
    type: text
    required: false
    hint: A description of the field for the editors
    localized: false
    # Enable or not the RTE [html|text]
    # text_formatting: html

- rating: # Name of the field
    label: Rating
    type: integer
    required: false
    hint: A 5 star rating of the recipe
    localized: false
    # Enable or not the RTE [html|text]
    # text_formatting: html

- shares: # Name of the field
    label: Shares
    type: integer
    required: false
    hint: Number of times has been shared
    localized: false
    # Enable or not the RTE [html|text]
    # text_formatting: html

- category: # Name of the field
    label: Shares
    type: hash
    required: false
    hint: Number of times has been shared
    localized: false
    # Enable or not the RTE [html|text]
    # text_formatting: html
