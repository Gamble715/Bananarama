# Public name of this content type
name: Sub menus

# TODO: explain
slug: sub_menus

# Explanation for the backoffice
description: A description of the content type for the editors

# Default field (e.g title)
label_field_name:  name

# Order of entries
order_by: manually # default: manually, also available: created_at or any field slug

# Order direction
# order_direction: asc # default: asc, also available: desc

# Display entries grouped by <field> in the backoffice
# group_by: <your field>

# Activate public 'create' API (e.g for a contact form)
# public_submission_enabled: false

# Emails to be notified on new entries using the public API
# public_submission_accounts: ['john@acme.net']

# Describe each field. The name shoud always be an underscored string
fields: 
-  name: # Name of the field
    label:  name
    type: string
    required: true
    hint: A description of the field for the editors
    localized: false

- path: # Name of the field
    label: Path
    type: string
    required: false
    hint: A description of the field for the editors
    localized: false

- page_title: # Name of the field
    label: Page title
    type: string
    required: false
    hint: A description of the field for the editors
    localized: false

- icon: # Name of the field
    label: Icon
    type: file
    required: false
    hint: A description of the field for the editors
    localized: false

- icon_height: # Name of the field
    label: Icon height
    type: integer
    required: false
    hint: A description of the field for the editors
    localized: false

- icon_width: # Name of the field
    label: Icon width
    type: integer
    required: false
    hint: A description of the field for the editors
    localized: false

- sort_order: # Name of the field
    label: Sort order
    type: integer
    required: false
    hint: A description of the field for the editors
    localized: false
